# 📝 文本对比工具 (Text Comparison Tool)

一款轻量级的字符级文本对比工具，基于 Python + Tkinter 构建。无需安装任何第三方依赖，支持打包为单文件 EXE 直接运行。

适用于文档校对、文案审核、翻译对照等场景。

---

## ✨ 功能特性

- **字符级对比** — 基于 `difflib.SequenceMatcher`，精确到每个字符的差异检测
- **差异可视化** — 新增内容以 🟢 绿色加粗显示，删除内容以 🔴 红色删除线显示
- **逐项导航** — 通过「上一项 / 下一项」按钮逐个浏览差异
- **上一项 / 下一项 / 拒绝** — 「上一项 / 下一项」仅逐个浏览差异；「拒绝」在修订文本中把该处还原为原文并前进
- **源文联动** — 原文/修订文本平时保持白底黑字、不做任何标记；选中某处差异时该片段变色突出（原文红删除线 / 修订绿加粗 + 蓝色底），无需手动定位
- **滚动同步** — 左右两个输入框按比例联动滚动，始终对照同一位置
- **点击跳转** — 在差异预览区点击任意差异项即可跳转选中
- **鼠标拖选** — 差异预览区支持鼠标滑选，选中部分蓝色高亮
- **一键粘贴 / 复制** — 每个输入框提供「粘贴」按钮；修订文本额外提供「复制」，处理完成后一键导出最终文本
- **Windows 11 外观** — 参照 Fluent Design 抗锯齿自绘圆角按钮与卡片（无毛边/白圈）、Win11 覆盖式滚动条（悬停展开、内容不满时自动隐藏）；顶部紧凑工具栏（对比、导航、拒绝、清空，统一间距），底部状态栏带快捷键提示，风格贴近系统记事本
- **键盘快捷键** — `Ctrl+Enter` 直接对比，`F1` / `F2` 上一项 / 下一项，`F4` 拒绝
- **高 DPI 适配** — 自动适配高分辨率屏幕
- **零依赖** — 仅使用 Python 标准库，无需安装第三方包

---

## 🚀 快速开始

### 直接运行

确保系统已安装 Python 3.8+：

```bash
python src/main.py
```

### 使用打包好的 EXE

从 [Releases](../../releases) 下载最新的 `文本校对.exe`，双击即可运行，无需 Python 环境。

---

## 📖 使用方法

1. 在左侧 **「原始文本」** 框中输入或粘贴原始内容
2. 在右侧 **「修订文本」** 框中输入或粘贴修改后的内容
3. 点击 **「开始对比」**（或按 `Ctrl+Enter`），差异预览区将展示所有差异
4. 使用 **「上一项 / 下一项」**（或 `F1` / `F2`）逐个浏览差异，或在原文/修订文本中点击差异片段；每选中一处，两侧文本框会自动滚动，并以蓝色底 + 红/绿样式突出对应片段
5. 需要还原某处差异时选择 **「拒绝」**（或按 `F4`），修订文本会将该处改回原文；「上一项 / 下一项」仅用于浏览定位
6. 处理完毕后，修订文本即最终结果，点击 **「复制」** 按钮导出；状态栏显示处理结果

---

## 🔧 打包构建

### 环境准备

```bash
# 创建虚拟环境
python -m venv .venv

# 激活虚拟环境 (Windows)
.venv\Scripts\activate

# 安装 PyInstaller
pip install pyinstaller
```

### 执行打包

```bash
pyinstaller --clean 文本校对.spec
```

打包完成后，EXE 文件位于 `dist/文本校对.exe`。

### 打包优化

当前 spec 文件已包含以下优化措施以减小体积：

- 排除未使用的标准库模块（`unittest`、`email`、`http`、`asyncio` 等）
- 字节码优化级别设为 2（移除 docstring 和 assert）
- 开启 `strip` 去除调试符号
- 开启 `upx` 压缩（需系统安装 [UPX](https://github.com/upx/upx/releases)）

---

## 📁 项目结构

```
Text_comparison/
├── src/
│   ├── main.py          # 入口：高 DPI 初始化 + 启动主循环
│   ├── app.py           # 主控制器：界面装配、对比流程、导航与状态
│   ├── theme.py         # 设计令牌：颜色、字体、间距
│   ├── drawing.py       # 抗锯齿圆角绘制（4×4 超采样角贴图）
│   ├── diff.py          # 差异引擎：Change 模型 + SequenceMatcher 封装
│   ├── widgets/         # 自绘控件包
│   │   ├── button.py    # FluentButton 圆角按钮
│   │   ├── scrollbar.py # FluentScrollbar Win11 覆盖式滚动条
│   │   └── card.py      # Card 无描边圆角面板
│   └── hook.py          # PyInstaller 运行时钩子
├── assets/
│   └── 文档对比.ico      # 应用图标
├── 文本校对.spec          # PyInstaller 打包配置
├── pyinstaller.txt       # 打包命令参考
└── README.md
```

---

## 🛠️ 技术栈

| 组件 | 技术 |
|------|------|
| 语言 | Python 3.8+ |
| GUI | Tkinter / ttk |
| 对比算法 | difflib.SequenceMatcher |
| 打包工具 | PyInstaller |

---

## 📄 许可证

本项目采用 [MIT License](LICENSE) 开源。
